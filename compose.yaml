version: '3.3'
services:
  server:
    ports:
        - '80:80'
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./Frontend-Vuetify/dist:/www
    environment:
      - ASPNETCORE_URLS=http://+:8080 #PORT
    depends_on:
      - db

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'db'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - '3306:3306'
    volumes:
      - ./test-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

volumes:
  db-data: